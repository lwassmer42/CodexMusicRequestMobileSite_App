import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vibecoding.musicrequesttracker',
  appName: 'Music Request Tracker',
  webDir: 'dist',
};

export default config;
